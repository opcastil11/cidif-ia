import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "CIDIF.TECH - Fund Application Platform",
    template: "%s | CIDIF.TECH",
  },
  description: "SaaS platform to help entrepreneurs and startups apply for public and private funds in LATAM, USA, and Europe with AI-powered assistance.",
  keywords: ["funding", "startups", "grants", "LATAM", "entrepreneurship", "AI assistant"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
